import React, { Component, PropTypes } from 'react';

export default class Quiz extends Component {
  static propTypes = {
    
  }
  
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        Hellooooo quiz
      </div>
    );
  }
}
